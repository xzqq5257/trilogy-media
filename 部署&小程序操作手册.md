# Trilogy 三合一应用 · 完整部署 & 小程序上线操作手册

> 应用：读书 / 听书 / 听音乐 + 声音克隆（上传音频/视频，模拟你的独特声源）
> 技术栈：后端 Node.js + Python（ffmpeg / edge-tts / librosa 声纹克隆）｜小程序 Taro 4 + React

---

## 第一部分：后端部署（先做这一步，获得永久 HTTPS 链接）

推荐 **Render 免费层**，无需绑卡，支持 Docker + Python + ffmpeg。

### 第 1 步：把代码推到 GitHub（已完成）

本地已执行：
```bash
cd /workspace
git add -A
git commit -m "chore(deploy): add render.yaml, railway.json, koyeb.yaml + miniapp"
git push https://xzqq5257:你的PAT@github.com/xzqq5257/trilogy-media.git main
```
✅ 仓库地址：`https://github.com/xzqq5257/trilogy-media`

### 第 2 步：注册 Render（1 分钟）

1. 打开 https://render.com
2. 点右上角「Sign up」→ 选「GitHub」直接登录（免费，免绑卡）
3. 授权 GitHub 仓库访问：允许访问 `trilogy-media`

### 第 3 步：一键部署（Blueprint 模式）

仓库根目录已包含 `render.yaml` Blueprint 配置（Docker 方式自动识别），不需要手动填参数：

1. Render Dashboard 点右上角 **New +** → **Blueprint**
2. 选择仓库 `xzqq5257/trilogy-media`，分支 `main`
3. 配置名随便填（例如 `trilogy-media`），点 **Apply**
4. 等待 **3~8 分钟**（Docker 拉取 ffmpeg + Python 依赖较慢）
5. 部署完成后，Service 名称下方有个类似 URL：
   ```
   https://trilogy-media-xxxx.onrender.com
   ```
   **复制并保存好，这个就是你的永久后端地址！**

> 🔔 免费层说明：每月 750 小时（够一整个月单人使用），空闲 15 分钟会休眠 → 第一次访问慢 30 秒是正常的。如果需要 24 小时不休眠 + 数据持久化磁盘（声源不会因重启丢失），升级到 7 美元/月的 Starter 计划即可。

### 第 4 步：验证后端可用

浏览器打开：
```
https://你的域名.onrender.com/api/health
```
返回 `{"ok":true}` 就成功了。

> 🚨 如果想用 Railway/Koyeb，仓库里已经有 `railway.json` 和 `koyeb.yaml`，操作流程一样：选仓库 → 自动识别配置 → 部署。

---

## 第二部分：注册微信小程序（免费，个人可做）

### 第 1 步：注册小程序账号

1. 打开 https://mp.weixin.qq.com/
2. 右上角「立即注册」→ 选「小程序」
3. 填邮箱（未绑定过微信公众平台的）→ 激活邮件 → 选择「个人」主体
4. 填姓名、身份证号，用绑定你本人银行卡的微信扫码完成实名（**免费，不扣费**）
5. 完成后进入小程序后台

### 第 2 步：获取 AppID

小程序后台 → 左侧「开发管理」→「开发设置」
- 复制你的 **AppID**（类似 `wx1234567890abcdef`），后面要用
- **开发者 ID 里也可以加你自己的微信号**，方便在手机上真机调试

### 第 3 步：配置服务器域名（上线前必须配，开发时可跳过）

小程序后台 → 「开发管理」→「开发设置」→「服务器域名」：

把以下 4 个字段全部填上你的后端域名（例如 `https://trilogy-media-xxxx.onrender.com`）：

| 字段名 | 填什么 |
|--------|--------|
| request 合法域名 | `https://你的域名.onrender.com` |
| uploadFile 合法域名 | `https://你的域名.onrender.com` |
| downloadFile 合法域名 | `https://你的域名.onrender.com` |

> ⚠️ **开发阶段**：打开微信开发者工具 → 详情 → 本地设置 → 勾选**「不校验合法域名、web-view(业务域名)、TLS 版本以及 HTTPS 证书」**，即可跳过这步先用 localhost 调本地后端。

---

## 第三部分：本地开发环境搭建（小程序端）

### 第 1 步：安装 Node.js 20

去 https://nodejs.org/ 下载 **LTS 版本**（推荐 Node 20，18 和 22 也可以）。装好后打开命令行验证：
```bash
node -v   # 应该看到 v20.x.x
npm -v    # 10.x
```

### 第 2 步：下载代码到你本机

两种方法任选：

**方法 A：Git Clone（推荐）**
```bash
cd 你放代码的目录
git clone https://github.com/xzqq5257/trilogy-media.git
cd trilogy-media/miniapp
```

**方法 B：ZIP 下载**
打开 https://github.com/xzqq5257/trilogy-media → 绿色 Code 按钮 → Download ZIP → 解压

### 第 3 步：安装依赖

```bash
cd trilogy-media/miniapp
npm install
```
（如果在国内网络慢，可以先 `npm config set registry https://registry.npmmirror.com`）

### 第 4 步：改后端地址配置

打开 `miniapp/config/` 目录：

**开发环境**（本地调试时用）：编辑 [miniapp/config/dev.ts](file:///workspace/miniapp/config/dev.ts)
```typescript
export default {
  env: {
    API_BASE: '"https://你的域名.onrender.com"',  // ← 改成你第一部分得到的 Render 域名
  },
  ...
}
```
> 💡 如果想本地调试，把你的本地电脑起的后端地址（`http://你电脑局域网IP:8787`）填进去也行，记得开发者工具里勾「不校验合法域名」。

**生产环境**（最终发布用）：编辑 [miniapp/config/prod.ts](file:///workspace/miniapp/config/prod.ts)
```typescript
export default {
  env: {
    API_BASE: '"https://你的域名.onrender.com"',  // ← 改成永久域名
  },
  ...
}
```

### 第 5 步：填入 AppID

打开 [miniapp/project.config.json](file:///workspace/miniapp/project.config.json)，把第 6 行：
```json
"appid": "touristappid",
```
改成你从微信后台拿到的 AppID，例如：
```json
"appid": "wx1234567890abcdef",
```

### 第 6 步：编译生成小程序代码

```bash
cd trilogy-media/miniapp
npm run build:weapp
```
看到 `webpack compiled successfully` 就是成功了。会在 `miniapp/dist/` 目录生成小程序代码。

---

## 第四部分：微信开发者工具导入与调试

### 第 1 步：下载微信开发者工具

打开 https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html → 下载**稳定版** → 安装

### 第 2 步：导入项目

1. 打开微信开发者工具 → 登录（用你注册小程序时扫码的微信号）
2. 点 **「导入项目」**
3. 依次填：
   - 项目名称：`Trilogy`
   - 目录：选择 `trilogy-media/miniapp/` 这个文件夹（注意不是 dist，是 miniapp 根目录）
   - AppID：选「使用测试号」或填你自己的 AppID
4. 点「确定」

### 第 3 步：开启调试开关（重要）

顶部菜单 → 详情 → **本地设置** 标签：
- ✅ 勾选「不校验合法域名、web-view、TLS 版本以及 HTTPS 证书」
- （可选）✅ 勾选「启用自动热重载」和「启用数据缓存」

### 第 4 步：开发调试

左侧模拟器会显示首页（首页/书架/音乐/声源 4 个 TabBar）。操作提示：

| 操作 | 说明 |
|------|------|
| 上传声源 | 点 Tab「声源」→ 虚线框区域点击上传，支持 mp3/wav/m4a/mp4 |
| 重新识别 | 每张声源卡片上的 🔄 按钮，重新提取声纹特征 |
| 试听/试读 | 声源卡片上的「试」和「读」按钮 |
| 选用某声源 | 点「选用」按钮，之后听书都用这个声读 |
| 听书 | Tab「书架」→ 选一本书 → 点「🎧 听书」，或 Tab「首页」→ 听书模块 |
| 读书 | Tab「书架」→ 点「📖 阅读」，进入阅读器 |
| 音乐 | Tab「音乐」→ 点击封面即播，右上角「＋」上传本地 mp3 |

**真机器调试**：点开发者工具右上角「预览」→ 手机扫二维码 → 真机上测试（手机上必须配服务器域名，否则接口调不通）

---

## 第五部分：小程序发布上线

### 第 1 步：重新编译生产版本

```bash
cd trilogy-media/miniapp
npm run build:weapp   # 自动用 prod 配置的永久域名
```

### 第 2 步：上传版本

微信开发者工具顶部 → **上传**：
- 版本号：`1.0.0`
- 备注：`Trilogy 首次发布`
- 点确认上传

### 第 3 步：提交审核

登录 https://mp.weixin.qq.com/ → 「版本管理」：

1. 「开发版本」里看到刚才上传的 → 点 **提交审核**
2. 填写版本描述（例如"读书/听书/音乐三合一应用，支持声音克隆"）
3. **类目选择**：个人主体可发布的类目如下（选一个对得上的）：
   - 「工具 / 效率」最稳，审核通过率最高
   - 或者「教育 / 阅读」、「文娱 / 音乐」
4. **隐私协议**：必须在「设置 → 基本设置 → 服务内容声明 → 用户隐私保护指引」里配置一个（模板一键生成即可，不用自己写）
5. 提交 → 一般 **1-2 小时** 就能通过审核

### 第 4 步：发布上线

审核通过 → 版本管理里点 **发布** → 全网用户可以搜索你的小程序名称访问了 🎉

---

## 第六部分：FAQ / 常见坑

### Q1：上传声源/音乐文件 0 字节 / 上传失败？
- 本地开发：检查是否勾了「不校验合法域名」
- 真机：服务器域名配置了吗？域名必须是 HTTPS（Render 默认就是，不用你配证书）

### Q2：听书没声音？
1. 先确认声源有选用：声源 Tab 有一张卡「选中」状态
2. 手机是否静音 / 调音量
3. 后端 `/api/tts/health` 返回 200 吗（Render 冷启动等 30 秒）

### Q3：重新构建后微信开发者工具没更新？
- 开发者工具里「工具」→「清缓存」→「全部清除」，再关闭工具重开
- 或者执行 `npm run build:weapp` 后点工具里的「编译」按钮

### Q4：Render 冷启动慢，用户体验差？
免费层这样是正常的。如果要一直在线：
- Render 升级到 7 美元 Starter
- 换 Railway（5 美元免费额度够用），仓库里已配 `railway.json`

### Q5：想改页面样式？
SCSS 文件在 `miniapp/src/` 下，改完 `npm run build:weapp` 重新编译即可生效。全局主题色在 [miniapp/src/app.scss](file:///workspace/miniapp/src/app.scss) 顶部的变量：
```scss
$accent: #7c5cff;  // 强调色（紫色）
$bg: #0d0e14;       // 背景色
```
改完重新编译就换肤了。

### Q6：后端数据（声源/上传的音乐）重启后丢失？
Render 免费层磁盘是临时的，这是免费套餐限制。解决：
1. 升级 Render Starter 计划（$7/月）→ 有 1G 持久化磁盘
2. 或者每次发布后重新上传声源

---

## 附：目录结构速查

```
trilogy-media/
├── miniapp/                      # 📱 小程序源码（Taro 4 + React 18）
│   ├── src/
│   │   ├── pages/
│   │   │   ├── home/             # 首页 Tab
│   │   │   ├── library/          # 书架 Tab
│   │   │   ├── music/            # 音乐 Tab
│   │   │   ├── voices/           # 声源 Tab（含重新识别🔄）
│   │   │   ├── reader/           # 阅读器（跳转页）
│   │   │   └── audiobook/        # 听书（跳转页，含实时字幕）
│   │   ├── store.ts              # Zustand 状态管理
│   │   ├── player.ts             # 音乐播放器（InnerAudioContext）
│   │   ├── api.ts                # API 封装
│   │   ├── lib/tts.ts            # 听书 TTS 引擎
│   │   └── app.config.ts         # 路由 + TabBar 配置
│   ├── config/
│   │   ├── dev.ts                # 开发环境 API_BASE
│   │   └── prod.ts               # 生产环境 API_BASE（改这里！）
│   └── project.config.json       # 微信 AppID 配置
│
├── server/                        # 💻 后端服务
│   ├── src/                      # Express API（8787端口）
│   └── voice_clone.py            # 声音克隆 Python 脚本
├── client/                        # 🌐 原 Web 版前端（备份）
├── Dockerfile                     # Render/Railway/Koyeb 自动构建用
├── render.yaml                    # Render Blueprint
├── railway.json                   # Railway 配置
└── koyeb.yaml                     # Koyeb 配置
```

祝上线顺利！🎉
